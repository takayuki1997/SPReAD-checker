# --- 文科省ツール／公式様式は再配布禁止のため公開しない ---
vendor/
samples/
baseline/
baseline_native_single/
baseline_pyodide/

# 念のため書類・様式の実体は一切コミットしない（安全策）
*.xlsx
*.xlsm
*.pdf
*.docx
# 検証スクショは実書類の内容を写し込み得るため追跡しない（毎回上書きされる生成物）
web/screenshot.png
# ただし vendor の .py は vendor/ ごと無視済み（当方コードの .py は対象外）

# --- 環境・生成物 ---
.venv/
node_modules/
__pycache__/
*.pyc
.DS_Store
package-lock.json
